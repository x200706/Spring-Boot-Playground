spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: SpringBootDB369
    url: jdbc:postgresql://db.xrizljdyaoyvzcavgbol.supabase.co:5432/postgres
  thymeleaf:
    mode: HTML
  debug: true
  jpa:
    # hibernate:
    #   ddl-auto: create # 連接數據庫結構，常用是update
    show-sql: true
  # mvc:
  #   view:
  #       prefix: /WEB-INF/jsp/
  #       suffix: .jsp
  # profiles:
  #   active: prod
logging:
  root: WARN
  level:
    org:
      springframework: DEBUG
  # file: logs/logs.log
  # path:
  # pattern:
  # config: classpath: 自定義文件<=可設定Rolling File
book:
  name: 書本一號